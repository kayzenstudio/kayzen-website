on:
  release:
    types: [published]
name: 🚀 Kayzen Website Release
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        server: ftp.kayzen.studio
        username: u561842527.root
        password: ${{ secrets.FTPCREDENTIALS }}
